import { HikeFilterPipe } from './hike-filter.pipe';

describe('HikeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HikeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
